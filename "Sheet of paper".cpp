#include <iostream>
#include <fstream>
using namespace std;


int main() {
    ifstream fin;
    ifstream fin2;
    ofstream fout;
    
    fout.open("output.txt",ios::app);
    
    fin.open("input1.txt");
    fin2.open("input2.txt");
    
    while(!fin.eof() || !fin2.eof())
    
    
    
    
  
  return 0;
}
